You are setting up a universal coding agent enhancement stack for this project. 
Execute every step in order. No explanations — just run and report status.

STEP 1 — KARPATHY SKILLS
curl https://raw.githubusercontent.com/forrestchang/andrej-karpathy-skills/main/CLAUDE.md >> CLAUDE.md
Verify: CLAUDE.md is non-empty.

STEP 2 — CONTEXT-MODE
npm install -g context-mode
cp $(npm root -g)/context-mode/configs/claude-code/CLAUDE.md /tmp/ctx-claude.md
cat /tmp/ctx-claude.md >> CLAUDE.md
Verify: context-mode doctor passes.

STEP 3 — POCOCK SKILLS
npx skills@latest add mattpocock/skills
Select ALL skills, install locally.
Then run: /setup-matt-pocock-skills
(Issue tracker: GitHub, Docs: docs/, Labels: defaults)

STEP 4 — FALLOW (codebase intelligence)
npx skills@latest add fallow-rs/fallow-skills
fallow setup-hooks
Verify: fallow --version responds.

STEP 5 — MEMPALACE
Read https://github.com/sirmews/mempalace README exactly.
Follow install instructions. Do not invent commands.
Verify: memory store initialised.

STEP 6 — MCP CONFIG
Create/update .claude/settings.json:
{
  "mcpServers": {
    "context-mode": { "command": "context-mode" },
    "fallow": { "command": "npx", "args": ["-y", "fallow-mcp"] }
  }
}

STEP 7 — VERIFY
Run each and confirm response:
1. context-mode doctor
2. fallow .
3. /caveman
4. ctx stats

Report final status:
[ ] Karpathy Skills — CLAUDE.md updated
[ ] context-mode — installed and doctor passing
[ ] Pocock Skills — all skills installed, setup complete
[ ] Fallow — installed, hooks set, MCP wired
[ ] MemPalace — memory store active
[ ] MCP config — context-mode + fallow wired
[ ] Caveman — active
[ ] ctx stats — responding
