You are setting up the ultimate coding agent enhancement stack for this project.
Your goal is to maximise three things: context engineering, memory engineering, and
agent harness engineering. Execute every step in order. No explanations — just run
and report status after each step.

═══════════════════════════════════════════════════════
LAYER 1 — BEHAVIORAL RULES (harness engineering)
═══════════════════════════════════════════════════════

STEP 1A — KARPATHY SKILLS
Run:
curl https://raw.githubusercontent.com/forrestchang/andrej-karpathy-skills/main/CLAUDE.md >> CLAUDE.md

STEP 1B — POCOCK SKILLS
Run:
npx skills@latest add mattpocock/skills
Select ALL skills, install locally.
Then run: /setup-matt-pocock-skills
  - Issue tracker: GitHub
  - Docs location: docs/
  - Labels: defaults

STEP 1C — BUILD SHARED DOMAIN LANGUAGE
Create CONTEXT.md in the project root:

---
# Project Domain Language

## What This Project Does
[Claude: read the codebase and write 2-3 sentences describing what this project does]

## Core Terminology
[Claude: scan the codebase and list all domain-specific terms, function names,
module names, and concepts that appear frequently — map each to a plain English
definition so future sessions don't waste tokens re-discovering them]

## Architecture Map
[Claude: describe the top-level structure — what each folder in src/ does,
what the entry points are, how data flows through the system]

## Key Decisions Already Made
[Claude: scan git history and code comments for any architectural decisions,
and document them here so future sessions don't re-debate them]
---

Verify: CONTEXT.md exists and is populated with real content from this codebase.

═══════════════════════════════════════════════════════
LAYER 2 — CONTEXT ENGINEERING
═══════════════════════════════════════════════════════

STEP 2A — CONTEXT-MODE
Run:
npm install -g context-mode
cp $(npm root -g)/context-mode/configs/claude-code/CLAUDE.md /tmp/ctx-claude.md
cat /tmp/ctx-claude.md >> CLAUDE.md
Verify: context-mode doctor — all checks pass.

STEP 2B — FALLOW
Run:
npx skills@latest add fallow-rs/fallow-skills
fallow setup-hooks
Verify: fallow --version responds, hooks registered.

STEP 2C — CONTEXT BUDGET RULES
Append to CLAUDE.md:

## Context Budget Rules
- Never read a file fully if only a specific function/section is needed — use grep first
- Never dump raw command output into context — use context-mode sandbox tools
- Before any multi-file operation, run: fallow . to get a structural map first
- Use ctx_batch_execute for any task requiring more than 2 shell commands
- Use ctx_fetch_and_index for any URL research — never raw WebFetch
- If context usage exceeds 40%, run: ctx stats and prune with ctx_purge if needed
- Maximum CLAUDE.md size: 100 lines. If it exceeds this, summarise and compress.

═══════════════════════════════════════════════════════
LAYER 3 — MEMORY ENGINEERING
═══════════════════════════════════════════════════════

STEP 3A — MEMPALACE
Read https://github.com/sirmews/mempalace README.
Follow exact install instructions. Do not invent commands.
Verify: memory store initialised.

STEP 3B — SESSION SCRATCHPAD
Create .claude/session-notes.md:

---
# Session Notes
_Updated by Claude at the end of every task._

## Active Tasks
- [ ] (none yet)

## Decisions Made
(log architectural and implementation decisions here)

## Known Issues
(log bugs found but not yet fixed)

## Completed
(move finished tasks here with a brief note)
---

STEP 3C — MEMORY BOUNDARY RULES
Append to CLAUDE.md:

## Memory Rules

### MemPalace — Session Working Memory (Claude Code only)
MemPalace handles short-to-medium term memory for active Claude Code sessions.
Write to MemPalace:
- What happened this session
- Code patterns discovered in this codebase
- Errors encountered and how they were fixed
- File locations of important logic
Do NOT write to MemPalace:
- High-level architectural decisions (goes to CONTEXT.md)
- Long-term project evolution (goes to Hermes)
- Cross-week patterns (goes to Hermes)

### Hermes — Project Lifecycle Memory (background agent)
Hermes handles long-term memory across days and weeks.
Write to Hermes memory:
- Architectural decisions and why they were made
- Patterns that repeat across multiple sessions
- Project evolution and milestone notes
- Anything that needs to be remembered in 2 weeks
Do NOT write to Hermes:
- What happened today (goes to MemPalace + session-notes)
- File-level implementation details (goes to MemPalace)

### session-notes.md — The Bridge
session-notes.md is the handoff layer between MemPalace and Hermes.
At the end of each session:
1. Claude summarises session into session-notes.md
2. Short-term detail → MemPalace
3. Long-term decisions → Hermes memory
Never write the same thing to both MemPalace and Hermes.
If unsure which layer: ask "will I need this in 2 weeks?" Yes → Hermes. No → MemPalace.

### General Memory Rules
- Session start: read .claude/session-notes.md and CONTEXT.md before anything else
- After every completed task: update .claude/session-notes.md
- New domain term discovered: add to CONTEXT.md immediately
- Architectural decision made: add to CONTEXT.md under Key Decisions
- Session end: summarise what changed at the top of session-notes.md

═══════════════════════════════════════════════════════
LAYER 4 — AGENT HARNESS ENGINEERING
═══════════════════════════════════════════════════════

STEP 4A — HERMES AGENT
Run:
curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
hermes mcp serve --background
Verify: hermes status confirms MCP server is active.

STEP 4B — WIRE ALL MCP SERVERS
Create/update .claude/settings.json:

{
  "mcpServers": {
    "context-mode": {
      "command": "context-mode"
    },
    "fallow": {
      "command": "npx",
      "args": ["-y", "fallow-mcp"]
    },
    "hermes": {
      "command": "hermes",
      "args": ["mcp", "serve"]
    }
  },
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Read(.env)",
      "Read(**/.env*)"
    ],
    "allow": [
      "Bash(git:*)",
      "Bash(npm:*)",
      "Bash(npx:*)",
      "Bash(fallow:*)",
      "Bash(context-mode:*)"
    ]
  }
}

STEP 4C — AGENT HARNESS RULES
Append to CLAUDE.md:

## Agent Harness Rules
- Before any feature work: run /grill-with-docs — no exceptions
- Before any bug fix: run /diagnose — define reproduce steps first
- All new features: use /tdd — write failing test first, then implement
- Every week: run /improve-codebase-architecture — prevent entropy
- If a task has more than 3 steps: write a plan first, get confirmation, then execute
- If uncertain about scope: use /zoom-out before touching any file
- Never modify more than one module boundary in a single session
- Hermes handles all background/scheduled tasks — do not block the IDE on long-running work
- If Hermes MCP tool routing fails: restart with `hermes mcp serve` — it is always the first suspect

## Session Start Checklist
1. Read .claude/session-notes.md
2. Read CONTEXT.md
3. Run: ctx stats (check context health)
4. Run: hermes status (check background agent)
5. Run: fallow . (get current codebase state)
Then ask: "What are we working on today?"

═══════════════════════════════════════════════════════
FINAL STEP — VERIFY EVERYTHING
═══════════════════════════════════════════════════════

Run each and confirm:
1. context-mode doctor
2. fallow .
3. hermes status
4. /caveman
5. ctx stats

Then output the final checklist:

[ ] LAYER 1 — Behavioral Rules
    [ ] Karpathy Skills        — CLAUDE.md updated
    [ ] Pocock Skills          — all installed, /setup complete
    [ ] CONTEXT.md             — built from real codebase content

[ ] LAYER 2 — Context Engineering
    [ ] context-mode           — installed, doctor passing
    [ ] Fallow                 — installed, hooks active
    [ ] Context budget rules   — appended to CLAUDE.md

[ ] LAYER 3 — Memory Engineering
    [ ] MemPalace              — memory store active
    [ ] session-notes.md       — created at .claude/session-notes.md
    [ ] Memory boundary rules  — appended to CLAUDE.md
    [ ] MemPalace scope        — session working memory only
    [ ] Hermes scope           — project lifecycle memory only
    [ ] session-notes.md       — confirmed as bridge layer

[ ] LAYER 4 — Agent Harness
    [ ] Hermes Agent           — installed, MCP server running
    [ ] MCP config             — all 3 servers wired in settings.json
    [ ] Permissions            — deny/allow rules set
    [ ] Harness rules          — appended to CLAUDE.md
    [ ] Session start checklist — in CLAUDE.md

[ ] Caveman                    — active
[ ] ctx stats                  — responding

If any step fails: read the actual error, do not guess,
report exactly what failed and why. Then stop and wait.
